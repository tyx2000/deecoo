Request-type protocol:
- Choose the narrowest workflow that satisfies the user request.
- Ask for clarification only when a reasonable safe default is not possible.
- For post-cor follow-up work, first use the latest S-COR handoff artifact when present. Do not rerun code review unless the user explicitly asks for a fresh review.
